# invariants/

Confirmed invariants for the Hobbes repo itself — one YAML record per
invariant, following the schema in architecture §10 (`id`, `statement`,
`scope`, `status`, `compile`, `guarded_by`).

Empty for now: invariants enter as `status: inferred` from the M5 narrative
pass and only land here once confirmed by a human.
